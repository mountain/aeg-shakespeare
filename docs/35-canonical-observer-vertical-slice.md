# Canonical observer vertical slice

**Status:** research-only API shaping; not part of the `0.0.2` compatibility contract.  
**Sonnet status:** the four-runner contact-depth axis is closed through an exact infinite-tail first-witness certificate.

## 1. Current causal order

The AEG Analysis / Shakespeare programme currently separates

```text
process direction
    -> local canonicalization when needed
    -> observer connection only when the canonical observer actually moves
    -> canonical decomposition
    -> minimum process-generated completion
    -> task-relative objectification
    -> persistent history placement / interleaving
    -> exact future-sufficiency certificate.
```

The final stages were forced by Sonnet 001 rather than imported from smooth theory.  The branch still contains only four reusable research roles:

```text
ProcessDirection
ConstraintCanonicalization
ObserverConnection
CanonicalDecomposition.
```

No generic `Completion`, `ResidualQuotient`, `ConstraintCell`, activation/reconvergence, persistent-DAG, or Huffman-action API has been promoted.

## 2. `ProcessDirection`

For a declared process frame `X_i`,

\[
\mathscr D=\sum_i u^iX_i.
\]

`ProcessDirection` acts on the assignment algebra and may be lowered to a one-generator `ProcessSystem`; ordinary ODEs are assignment shadows rather than the process ontology.

Executable controls:

```text
tests/classical/test_am_process_direction.py
tests/classical/test_restricted_riccati_canonical_observer.py
tests/classical/test_coupled_scalar_canonical_observer.py
```

A/M is a negative control: process motion alone does not imply observer transport.

## 3. `ConstraintCanonicalization`

The first implemented backend uses exact local conditions

\[
\Phi(z,g)=0.
\]

Differentiate along declared base rates and solve uniquely for observer rates.  There is deliberately no generic `Canonicalization` protocol: restricted Kepler remains the red team against assuming osculation, orthogonality, projection, stationarity, and exact-equation gauges share one implementation contract.

## 4. `ObserverConnection`

`ObserverConnection` records actual observer motion required to preserve a local canonical representation.  Current positive calibrations remain continuous:

- restricted Riccati: root/separation parameters move inside an affine observer family;
- coupled scalar registers: relative scale moves while preserving balanced cross coupling.

It stores provenance, base rates, observer rates, and exact residuals.  It does not define principal-bundle structure, curvature, holonomy, composition, or numerical path ordering.

### Sonnet negative control

At both center-2 -> 3 and center-3 -> 4, the apparent nonbranching task changes preserve witness boundary and mode and shift only event rank by `+2`.  They are history/decoder reindexing, not observer motion.

Therefore Sonnet 001 supplies

\[
\boxed{\text{no discrete evidence for `ObserverConnection`.}}
\]

## 5. `CanonicalDecomposition`

The reusable result shape is

\[
F=F_{\rm ren}+F_{\rm res}+F_{\rm comp},
\]

with domain-specific evidence and no universal discovery algorithm.

| Calibration | Carrier | Exact split |
| --- | --- | --- |
| restricted Riccati | Lie directions | affine tangent / no resonance / `Q` completion |
| coupled scalar registers | multivariable Lie directions | diagonal ruler / no resonance / cross completion |
| restricted Kepler | finite function module | `n=0 / n=1 / n=2` |
| Sonnet center 2 -> 3 | persistent task states | `843 / 0 / 6` |
| Sonnet center 3 -> 4 | persistent task cells | `2746 / 0 / 7` |

The empty middle Sonnet sector is evidence, not an omitted feature.

## 6. Classical calibrations remain the API anchors

### Restricted Riccati

With

\[
A=\partial_x,
\quad M=x\partial_x,
\quad Q=x^2\partial_x,
\]

an affine root/separation observer absorbs the affine directions while `Q` survives as completion.  Only afterwards does the executable essay identify

\[
[A,M]=A,
\qquad[A,Q]=2M,
\qquad[M,Q]=Q
\]

with the classical Riccati `sl(2)` realization [Carinena-Marmo-Nasarre-1998; Hall-2015].

### Coupled scalar registers

The balancing condition

\[
b_{12}\rho^2-b_{21}=0
\]

gives

\[
\frac{\dot\rho}{\rho}
=\frac12\left(
\frac{\dot b_{21}}{b_{21}}-
\frac{\dot b_{12}}{b_{12}}
\right).
\]

With repository convention `[X,Y]=X(Y)-Y(X)`, the executable sign is

\[
[E_{12},E_{21}]=M_2-M_1.
\]

The opposite-sign line in the externally supplied AEG Analysis v0.2 note remains recorded for correction [Hall-2015].

### Restricted Kepler

For

\[
\mathcal K_1=\operatorname{span}\{1,\cos\psi,\sin\psi\},
\qquad L_K=R^2+1,
\]

the exact `n=0`, `n=1`, `n=2` forcing sectors calibrate renormalization, resonance/modulation, and function-module completion without forcing osculation through `ConstraintCanonicalization` [Goldstein-Poole-Safko-2002; Arnold-1989; DLMF-4.21].

## 7. Sonnet Phase 8 — completion, objectification, and history placement

Center-2 -> center-3 gives

\[
\boxed{843F_{\rm ren}\oplus0F_{\rm res}\oplus6F_{\rm comp}.}
\]

The six completion parents have minimum raw wall supports

\[
1,2,2,2,3,4,
\]

all genuinely new at center 3.  Two minimum raw grammars still over-refine the task and require exact objectification

\[
11\to7,
\qquad13\to3.
\]

Thus

```text
completion pressure
!= minimum process-generator support
!= minimum task representation.
```

A local graft has `376 tree / 125 internal / 200 terminal-merged DAG` but `peak/worst=75/12`.  A fresh center-3 tree has the same `376/125` inventory and `72/10`, proving that primitive decision content and history placement are different problems.

Controlled interleaving over only the 21 old walls plus seven generated center-3 walls yields 2,753 exact feasible items / 75 tasks and recovers

```text
weighted depth 135
376 tree / 125 internal / 200 DAG
peak/worst 72/10.
```

The placement improvement comes from partial-order/sharing changes, including cross-parent activations, not extra completion primitives.

## 8. Cost semantics remain multi-axis

The historical 55-input current-use distribution misses every Phase-8 refinement-sensitive state.  Conditional on 298 actually reopened children, the exact new-wall workload is

\[
544/298\approx1.8255
\]

queries per reopened child, or `17/9` per genuine completion child, worst extra depth three.

Old-prefix reweighting can improve future depth while exploding frontier width.  Therefore current-use time, future-refinement time, and frontier/boundary geometry remain separate cost axes.  No scalar future-cost field is frozen.

## 9. Sonnet Phase 9 — frozen-rule scaling succeeds

The Phase-8E 28-predicate state materializes as

\[
2753\text{ cells},\quad75\text{ tasks},\quad13609\text{ exact closure atoms}.
\]

A generalized next-layer detector first replays the exact center-2 -> 3 `841/2/6` sets.  Applied unchanged to center-3 -> 4 it marks

\[
\boxed{2744\text{ stable}+2\text{ nonbranching}+7\text{ completion}.}
\]

Only `9/2753≈0.327%` of the current representation is reopened.

### Semantic correction and independent oracle

The seven completion cells each have exactly three center-4 task semantics.  A stale intermediate essay had recorded `2,2,3,3,4,4,4`; that assertion was rejected by current-head consistency checking and an independent local full-stratum oracle.

The lazy semantic oracle may query latent older walls, but Phase 9C eliminates them from every minimum task support.  Hence

\[
\boxed{\text{certificate/solver trace}\neq\text{visible task ontology}.}
\]

### One shared completion primitive

All seven completion cells have minimum support size one and choose the same genuinely new ternary wall:

\[
\boxed{\frac{u_4}{u_3}\ ?\ \frac{19}{11}.}
\]

In every branching cell, signs `-1,0,+1` map one-to-one to its three task semantics.  Every minimum support has `(new,latent)=(1,0)`.

## 10. Phase 9D — the center-4 persistent representation

Appending the discovered wall globally gives

```text
center 3
  cells/tasks               2753 / 75
  tree/internal/DAG          376 / 125 / 200
  peak/worst/weighted         72 / 10 / 135

center 4
  cells/tasks               3067 / 81
  closure atoms             14967
  tree/internal/DAG          391 / 130 / 211
  peak/worst/weighted         75 / 10 / 135.
```

The whole center-4 semantic update adds only

\[
\boxed{5\text{ internal decisions},\quad11\text{ DAG objects},\quad6\text{ tasks}}
\]

while current weighted depth stays exactly 135.  The new wall occurs at five internal nodes, two cross-parent, earliest depth six.

## 11. Phase 10 — finite presentation closes an infinite future tail

The unchanged center-4 -> 5 pressure detector returns

\[
\boxed{3067\text{ stable}+0+0.}
\]

Direct semantic red team compares all eight center-5 enter/exit events on all 14,967 closure atoms against the current first witness:

\[
14967\times8=119736.
\]

Every comparison is strictly later; none is earlier/equal or unresolved.

A stronger finite certificate uses only the earliest future event for each runner, center-5 enter:

\[
\alpha_{5,\mathrm{enter}}=5-\delta=\frac{24}{5}.
\]

Across

\[
14967\times4=59868
\]

anchor comparisons, every event is strictly later than the witness.  For every integer `n>=5` and either event kind,

\[
\alpha_{n,\mathrm{kind}}\ge\frac{24}{5}.
\]

Since runner speeds are positive, every later event for the same runner occurs still later.  Therefore

\[
\boxed{
\text{the finite 29-predicate center-4 presentation is first-witness sufficient for the entire infinite remaining contact alphabet.}
}
\]

This is the natural endpoint of the Sonnet contact-depth axis.

## 12. API consequence

The strongest new evidence is about presentation semantics, not new object taxonomy:

```text
open-ended process history
-> task quotient
-> sparse generated completion
-> task objectification
-> history interleaving/reconvergence
-> repeated frozen-rule update
-> finite semantic closure of an infinite future tail.
```

This supports the idea that a process presentation can become a finite task-sufficient statistic for an infinite continuation.

It does **not** yet justify public `ConstraintCell`, `PersistentDAG`, `ActivationPolicy`, or `Completion` abstractions.  Exact closure provenance is a research/certificate backend; Phase 9C shows it can be richer than the task-minimal visible state.

## 13. Negative controls and next promotion gate

Current negative controls include:

- A/M process motion without observer motion;
- Pendulum scalar observable selection without dynamic observer state;
- Sonnet history reindexing without observer transport;
- minimum raw completion without task minimality;
- current-use depth without future-refinement cost;
- zero-collateral activation without cross-parent sharing;
- solver/certificate wall queries without minimum-representation necessity.

The contact-depth axis is now closed.  The next useful discrete experiment should be an **unrelated process problem** that independently pressures generated completion + task objectification + history interleaving/reconvergence.  Only after such a second calibration should a public activation/reconvergence abstraction be considered.

Separately, the observer-ODE programme should return to a problem with a genuinely moving local canonical frame.  Sonnet 001 is now a strong negative control against conflating history geometry with `ObserverConnection`.

`K=13` remains a frozen holdout.

## 14. Executable evidence

```text
tests/research/test_lonely_runner_center4_constraint_cells.py
tests/research/test_lonely_runner_center4_semantic_redteam.py
tests/research/test_lonely_runner_center4_minimal_completion.py
tests/research/test_lonely_runner_center4_persistent_update.py
tests/research/test_lonely_runner_infinite_contact_tail_closure.py
```

The full Phase 9–10 argument and run ledger are archived in
`sonnet/lonely-runner/28-phase9-10-center4-scaling-and-infinite-tail-closure.md`.

## 15. References

[Hall-2015] Brian C. Hall, *Lie Groups, Lie Algebras, and Representations: An Elementary Introduction*, 2nd ed., Springer, 2015; DOI 10.1007/978-3-319-13467-3.

[Carinena-Marmo-Nasarre-1998] J. F. Carinena, G. Marmo, J. Nasarre, "The nonlinear superposition principle and the Wei-Norman method," arXiv:physics/9802041 (1998), https://arxiv.org/abs/physics/9802041 .

[Arnold-1989] V. I. Arnold, *Mathematical Methods of Classical Mechanics*, 2nd ed., Springer, 1989; DOI 10.1007/978-1-4757-2063-1.

[Goldstein-Poole-Safko-2002] Herbert Goldstein, Charles P. Poole Jr., John L. Safko, *Classical Mechanics*, 3rd ed., Addison-Wesley, 2002, Chapter 3, ISBN 0-201-65702-3.

[DLMF-4.21] NIST Digital Library of Mathematical Functions, §4.21, https://dlmf.nist.gov/4.21 .

[Huffman-1952] David A. Huffman, "A Method for the Construction of Minimum-Redundancy Codes," *Proceedings of the IRE* 40(9) (1952), 1098--1101; DOI 10.1109/JRPROC.1952.273898.

[Sungkawichai-Trakulthongchai-2026] Touch Sungkawichai, Tanupat Trakulthongchai, "Eleven, twelve, and thirteen lonely runners," arXiv:2604.23906 (2026), https://arxiv.org/abs/2604.23906 .
