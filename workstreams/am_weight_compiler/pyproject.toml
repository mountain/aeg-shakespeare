[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "am-power-weight-compiler"
version = "0.1.0"
description = "Research-local observer-directed completed AM coefficients"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
