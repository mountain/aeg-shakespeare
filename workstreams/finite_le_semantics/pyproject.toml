[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "process-geometry-finite-le-semantics"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = ["sympy>=1.13,<2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
